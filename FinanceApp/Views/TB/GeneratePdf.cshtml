@model FinanceApp.Models.LRModel

@{
    ViewData["Title"] = "Generate Neraca";
    var msg = ViewBag.Message;
}
<style>
    input.largerCheckbox {
        width: 20px;
        height: 20px;
    }
</style>
<h1>Download Neraca</h1>
<hr />
<form asp-action="GeneratePdf" id="formcreate" enctype="multipart/form-data">
    <div class="container">
        <div class="row">
            <div class="col-sm">
                <div class="form-group">
                    <label class="control-label">Tahun:</label>
                    <input asp-for="year" class="form-control" />
                    <span asp-validation-for="year" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="button" value="Download Report" onclick="alertfirst()" class="btn btn-red" />
                </div>

            </div>
        </div>
    </div>
</form>


<div>
    <a asp-action="Index" class="btn btn-dark">Kembali ke List</a>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        $(document).ready(function () {
            var msg = '@msg';
            if(msg != "" ){
                alert(msg);
            }

        });

        function allowOnlyNumber(evt) {
            var charCode = (evt.which) ? evt.which : event.keyCode
            if (charCode > 31 && (charCode < 48 || charCode > 57))
                return false;
            return true;
        }
        function alertfirst() {
            $('#formcreate').submit();

            // if (confirm("Are you sure saving data?")) {
            //     $('#formcreate').submit();
            // } else {
            //     return false;
            // }

        }
    </script>
}


